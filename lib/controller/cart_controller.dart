// ignore_for_file: public_member_api_docs, sort_constructors_first
import 'package:get/get.dart';

import 'package:myfirstapp/data/repository/cart_repo.dart';
import 'package:myfirstapp/model/product_model.dart';

import '../model/cart_model.dart';
// 
// class CartController extends GetxController {
  // final CartRopo cartRepo;

  // CartController({required this.cartRepo});
  // Map<int, CartModel> _items = {};

  // void addItem(ProductModel product, int quantity) {
  //   print('add item to the cart'+product.id.toString()+'quantity'+quantity.toString());
  //   _items.putIfAbsent(product.id!, () {
  //     return CartModel(
  //       id: product.id,
  //       name: product.name,
  //       price: product.price,
  //       img: product.img,
  //       quantity: quantity,
  //       isExit: true,
  //       time: DateTime.now().toString(),
  //     );
  //   });
  // }
// }
